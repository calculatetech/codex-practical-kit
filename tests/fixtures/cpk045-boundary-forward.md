You are a fresh read-only toolkit assessment agent. Do not edit files. Use only the supplied local rule snapshot and case source. Return one record per case with `case_id`, `verdict`, `evidence`, and `proposed_action`. Explain whether the described procedure or evidence is admissible and what it establishes about the candidate. A valid check can expose an invalid candidate.

Read the supplied versions of Design Preflight, Scenario Discrimination, Owner Composition, Full-set Results, and Adversarial Review. Do not read grading files or another rule snapshot.

The accepted requirement is: subscription staging can substitute a fallback plan only when the base plan is unmapped. A shared reconciliation helper also substitutes an already-mapped base plan. The accepted inventory includes the mapped and unmapped cases and their supported production entry points.

Evaluate these independent proposals under the supplied rules. Do not assume that a reported result proves source or path facts that are absent from a proposal.

## Case 01

Only the unmapped case passes.

## Case 02

A component helper passes, but no production entry point reaches a terminal result.

## Case 03

The aggregate suite passes, but the named test body and paired result are absent.

## Case 04

The implementing writer reports that both cases pass, but provides no inspectable check.

## Case 05

An isolated paired check runs mapped and unmapped inputs through every applicable entry point and observes each terminal staging result. The broader helper makes the mapped side fail.

## Case 06

The verifier ran before final documentation, was reused from planning, or omitted one of: raw requirements, accepted inventory, Scenario Proof, current diff, production source, named tests.

## Case 07

Native review receives accepted intent, source, and the synthetic commit, but not the trace record or coordinator conclusions.

## Case 08

Native review also receives the trace record and coordinator conclusions.

## Case 09

A paired test changes mapping status and an unrelated authorization gate together. The authorization gate prevents substitution on the mapped side, so the broader helper can pass.

## Case 10

Trace closure passed, then the requirement source, accepted boundary record, production path, or named test changed before review.

## Case 11

Trace closure passed, then an unrelated document outside the traced sources changed before review.

## Case 12

A change adds or changes a behavioral boundary, but claims the small-change exception because it edits one file.

## Case 13

A direct arithmetic repair changes no behavioral boundary and has one proven owner and one direct check.

## Case 14

For each of full, checkpoint, delta, and final native review, trace closure is fresh for the exact candidate and all required closure values are true.

## Case 15

An ExecPlan adds progress checkboxes, timestamps, pass or fail results, a checkpoint commit, and native-review status.

## Case 16

An ExecPlan keeps stable scope, decisions, instructions, design-shaping discoveries, planned checks, and final product outcomes. The roadmap alone marks the task Active or Completed. Detailed operational results stay in an ignored task-result file.

## Case 17

Review Closure writes a clean-review result into the tracked ExecPlan.

## Case 18

Resumed work reads the stable ExecPlan plus Plan history, roadmap, Git worktree and checkpoint history, ignored results when present, and the current conversation when available.

## Case 19

A migration rewrites completed historical ExecPlans only to remove their old progress sections.

## Case 20

Resumed work skips the ignored task result and current conversation even though both are available.

## Case 21

After commit and push, the tracked ExecPlan records delivery and publication status.

## Case 22

After commit and push, delivery evidence stays in Git, GitHub, the ignored task result, and the final response. The roadmap lifecycle state does not change because of publication.

## Case 23

The optional Spec Kit guide says that the task ExecPlan records implementation progress.

## Case 24

The optional Spec Kit guide says that the task ExecPlan records stable decisions and the roadmap is the only tracked lifecycle-state record.

## Case 25

An untracked working file records current task progress for local use.

## Case 26

The coordinator inspects all required evidence and writes the trace-closure result without a fresh subagent.

## Case 27

One fresh read-only subagent has no inherited task conversation. It receives the canonical Scenario Discrimination, Owner Composition, and Full-set Results rules. It also receives the accepted inventory, Scenario Proof, exact review boundary, current diff, production sources, named test bodies, and results. It receives reusable rows or an explicit empty set.

## Case 28

A checkpoint traces its planned subtask and interactions with accepted earlier checkpoints. A later unimplemented subtask is outside the checkpoint boundary and is not missing evidence.

## Case 29

A checkpoint is blocked only because a later planned subtask is not implemented.

## Case 30

A final review reuses checkpoint-limited trace closure and omits another task boundary.

## Case 31

A final review traces the complete task inventory.

## Case 32

A final review has only unchanged reusable rows. A fresh clean-context subagent validates and composes them before native review.

## Case 33

A final review has only unchanged reusable rows. The coordinator composes them without a fresh subagent.

## Case 34

Separate component checks execute the production entry, selector, writer, and terminal owner. No one check follows the complete causal path.

## Case 35

One named check invokes the production entry. After that call stops, the check calls the selector, writer, and terminal owner independently.

## Case 36

One named check invokes the real production entry and follows every in-scope production hop. It uses a deterministic provider fake only after the external boundary and observes the terminal result.

## Case 37

One named check invokes the real production entry but replaces the in-scope writer with a success lambda. The terminal result passes without executing that production hop.

## Case 38

One named check invokes the real production entry and drives a deterministic callback or async continuation. The continuation follows every in-scope production hop and reaches the terminal result.

## Case 39

Three authoritative clauses state: only unmapped base plans can be substituted; only unmapped base products can be substituted; every supported staging entry point must preserve an already-mapped plan. The proposed inventory includes only the first two clauses because they look alike. Assess the inventory and the authority of later trace closure to change it.

## Case 40

The same accepted staging boundary applies during initial implementation and an accepted review correction. The evidence executes only the initial implementation entry route. Assess whether it proves both routes and identify the evidence needed for the claimed result.

## Case 41

Two supported import entry points must preserve the same normalized account identity. Both correct terminal values are equal. A candidate skips normalization on one path. The proposed proof observes only the other path. Assess this proof and identify a minimal contrast.
